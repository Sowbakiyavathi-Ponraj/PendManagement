import { Applicationconfiguration } from './applicationconfiguration';

describe('Applicationconfiguration', () => {
  it('should create an instance', () => {
    expect(new Applicationconfiguration()).toBeTruthy();
  });
});
